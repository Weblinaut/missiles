/*
var express = require('express'),
	router = express.Router();

/!* GET Settings page. *!/
router.get('/', function(req, res, next) {
	res.render('index', {
		title: 'Missile Command - Settings',
		layout: './templates/masterTemplate'
	});
});

module.exports = router;
*/
